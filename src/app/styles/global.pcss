html {
  font: var(--baseFontWeight) var(--baseFontSize) / var(--baseLineHeight)
    var(--baseFontFamily);
}

h1, h2, h3, h4 {
  padding: 0;
  margin: 0;
}

h3 {
  font-size: 20px;
}

p {
  font-size: 14px;
}

button {
  padding: 0;
}